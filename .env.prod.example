# Production Environment Configuration
# Copy this file to .env and fill in your values

# Domain Configuration
DOMAIN=almeyo.com

# SSL Certificate Configuration
CERT_EMAIL=admin@almeyo.com

# Email Configuration (for contact/reservation forms)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@almeyo.com

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Logging
LOG_LEVEL=info

# Database
DATABASE_PATH=./data/almeyo.db

# CORS - Update with your actual domain(s)
CORS_ORIGIN=https://almeyo.com,https://www.almeyo.com

# Notes:
# - For Gmail: Enable "Less secure app access" or generate an app-specific password
#   https://myaccount.google.com/apppasswords
# - CERT_EMAIL should be a valid email for Let's Encrypt certificate notifications
# - DOMAIN must match your DNS configuration and SSL certificate domain
# - DATABASE_PATH must be writable and the data directory must exist
# - CORS_ORIGIN should include all domains that need to access your API
