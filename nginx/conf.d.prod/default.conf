# This directory contains nginx configuration files for production
# The main configuration is in nginx.prod.conf
# Additional per-location or per-upstream configs can be placed here

# Example: separate SSL configuration
# include /etc/nginx/conf.d/ssl.conf;

# Rate limiting and security policies
# include /etc/nginx/conf.d/security.conf;
