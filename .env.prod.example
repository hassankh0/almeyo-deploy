# Example Environment File for Production Deployment
# Copy this file to .env.prod and update with your actual values
# IMPORTANT: .env.prod must NEVER be committed to git - add it to .gitignore

# ============================================
# Domain Configuration
# ============================================
DOMAIN=your-domain.com
ENVIRONMENT=production

# ============================================
# Email Service (SMTP)
# ============================================
# Gmail example:
# 1. Create an App Password: https://myaccount.google.com/apppasswords
# 2. Use the generated password below
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_FROM=noreply@almeyo.com

# Alternative: Use your own mail server
# SMTP_HOST=mail.example.com
# SMTP_PORT=587
# SMTP_USER=admin@example.com
# SMTP_PASS=your-password

# ============================================
# Node.js Environment
# ============================================
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# ============================================
# Logging
# ============================================
LOG_LEVEL=info
# Options: error, warn, info, debug
LOG_PATH=/app/logs

# ============================================
# Database
# ============================================
# SQLite (default)
DATABASE_PATH=/app/data/almeyo.db

# ============================================
# CORS Configuration
# ============================================
# Update with your actual domain(s)
# Comma-separated for multiple domains
CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# ============================================
# SSL/TLS Configuration
# ============================================
# For Let's Encrypt certificate generation
CERT_EMAIL=admin@your-domain.com
CERT_DOMAIN=your-domain.com

# ============================================
# API Configuration
# ============================================
# Request timeout in milliseconds
API_TIMEOUT=30000

# Maximum upload file size
# Format: 50m, 100M, 1G, etc.
MAX_UPLOAD_SIZE=50m
